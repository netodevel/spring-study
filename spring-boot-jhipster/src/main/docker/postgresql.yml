version: '2'
services:
  boilerplate-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/boilerplate/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=boilerplate
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
